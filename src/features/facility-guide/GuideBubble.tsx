import { GuideActions } from './GuideActions';
import type { GuideActionId, GuideMessage } from './guideTypes';
export function GuideBubble({ message, onAction, onClose, onDismiss, onSettings }: { message: GuideMessage; onAction: (id: GuideActionId) => void; onClose: () => void; onDismiss: () => void; onSettings: () => void }) { return <section className="guide-bubble" role="status" aria-live="polite"><div className="guide-bubble-tools"><button onClick={onSettings} aria-label="Guide settings">⚙</button><button onClick={onClose} aria-label="Close guide">×</button></div><small>⚡ FACILITY GUIDE</small><h2>{message.title}</h2><p>{message.body}</p><GuideActions actions={message.actions} onAction={onAction} /><div className="guide-dismiss-row"><button className="guide-dismiss" onClick={onClose}>Snooze</button><button className="guide-dismiss" onClick={onDismiss}>Don’t show this tip again</button></div></section>; }
