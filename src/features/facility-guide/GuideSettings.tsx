import { useFacilityGuide } from './useFacilityGuide';
export function GuideSettings() { const guide = useFacilityGuide(); const p = guide.preferences; return <section className="guide-settings" role="dialog" aria-label="Facility Guide settings"><header><b>Guide settings</b><button onClick={() => guide.setSettingsOpen(false)}>×</button></header><label><input type="checkbox" checked={p.enabled} onChange={(e) => guide.setPreferences({ ...p, enabled: e.target.checked })} /> Guide on</label><label><input type="checkbox" checked={p.automaticTips} onChange={(e) => guide.setPreferences({ ...p, automaticTips: e.target.checked })} /> Automatic tips</label><label><input type="checkbox" checked={!p.muted} onChange={(e) => guide.setPreferences({ ...p, muted: !e.target.checked })} /> Sound</label><label>Animation<select value={p.animationMode} onChange={(e) => guide.setPreferences({ ...p, animationMode: e.target.value as typeof p.animationMode })}><option value="full">Full</option><option value="reduced">Reduced</option><option value="off">Off</option></select></label><button onClick={guide.reset}>Reset guide and tutorial</button></section>; }

