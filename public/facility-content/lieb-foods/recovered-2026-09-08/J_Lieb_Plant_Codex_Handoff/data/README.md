# Data tables

These are review/ingestion staging tables, not the repository schema and not authoritative live configuration. JSON preserves raw display strings and CSV provides inspectable equivalents. Candidate recovery keys are not new canonical asset IDs. Resolve source IDs through source_register.json. Do not auto-import disputed electrical relationships.
